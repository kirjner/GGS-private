seq_len: 7 #11^2 for diamond
task: Diagonal
diagonal:
  csv_path: ${paths.data_dir}/Diagonal/landscape_ep.csv
  output_dir: ${paths.data_dir}/Diagonal/
  filter_percentile: [0.0, 1.0]
  min_mutant_dist: 0
  top_quantile: 0.99
  use_levenshtein: True
  use_neg_data: False
  smoothed_data_fname: null 

